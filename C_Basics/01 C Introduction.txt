1.C Keywords and Identifiers:
    Character sets => Alphabets, Digits, Special Characters, White space Characters.
    Keywords
    Identifiers
2.Variables and Constants:
    Variables
    Literals => 
        Integers => decimal, octal( starts with 0 ), hexadecimal( starts with 0x )
        Floating Point
        Characters
        String Literals
        Escape Sequences => \b	\f	\n	\r	\t	\v	\\	\' \" \? \0
    Constants
3.Data Types:
    int   ( at least 2, usually 4 ) %d, %i
    char  ( 1) 	%c
    float ( 4)	%f 
    double(	8)	%lf
    void
    NULL

    type specifier => short, long, signed, unsigned
    other types => Enumerated type, Complex types, bool ( stdbool.h )

4.Input and output:
    printf(), scanf(),
    getc(), getchar(), gets()
    putc(), putchar(), puts()

5.Operator:
    Arithmetic => + - * / %
    Assignment => += -= *= /= %= 
    In-De crement => ++ --
    Relational => == != <= < >= >
    Logical => && || !
    Bitwise => & | ^ ~ >> <<
    sizeof => sizeof()
    comma operator => ,
    ternary operator ?:
    reference operator &
    dereference operator * 
    member selection operator ->
